class Math {

    // 두배
    static double twoTime(double num) {
        return num * 2;
    }

    // 절반
    static double half(double num) {
        return num / 2;
    }

    // 강화된 알고리즘
    static Double doubled(Double d) {
        return d * 2;
    }
}
